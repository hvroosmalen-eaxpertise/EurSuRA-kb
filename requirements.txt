# Site build (required to build/serve the wiki)
mkdocs>=1.6,<2.0
mkdocs-material>=9.5
mkdocs-git-revision-date-localized-plugin>=1.2   # per-page "last update" in the footer

# Ingestion pipeline (kb-framework/pipeline/ingest.py)
anthropic>=0.40
pyyaml>=6.0
python-dotenv>=1.0
pypdf>=4.0          # PDF text-extraction fallback when marker-pdf is unavailable
