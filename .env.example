# Copy to .env and fill in. Loaded by the ingestion pipeline (ingest.py).
# .env is git-ignored — never commit real keys.

# Required for Claude enrichment during PDF ingestion.
ANTHROPIC_API_KEY=sk-ant-...
